import { HStack, Stack, Text } from "@chakra-ui/react";

function NavBar() {
  return (
    <HStack justifyContent="center">
      <Text textStyle="h1" color="brand.100">
        Headline.
      </Text>
    </HStack>
  );
}

export default NavBar;
